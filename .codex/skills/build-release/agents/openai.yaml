interface:
  display_name: "Build Release"
  short_description: "Version, build, and audit GraDOOM releases"
  default_prompt: "Use $build-release to select the next GraDOOM version and build a validated release candidate."
