interface:
  display_name: "Build Release"
  short_description: "Publish and verify GraDOOM releases on PyPI"
  default_prompt: "Use $build-release to publish the next GraDOOM version and verify it on PyPI."
